# Guest-side runtime pieces needed by skcov tests/build helpers.
# Note: Buildroot does not model Debian-style `-dev` target packages; headers
# and static libs are not installed into the target rootfs.
BR2_PACKAGE_PYTHON_CFFI=y
BR2_PACKAGE_ELFUTILS=y
